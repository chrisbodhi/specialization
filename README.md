# Specialization is for insects

## Getting Started

To use Rust, &c:

1. Run `nix-shell` in this directory
2. Rust, &c, will be available in the shell environment

The shell will display the versions of  `rustc` and `zola` when you enter it.

### Running

`zola serve`

## Publishing

### For the web

`zola build`
